# 2026-09-13 执行日志

## 公开源码仓库与品牌更换

- 用户确认“选中短句”Logo 方向：四角捕获框、三行信息流、青柠高亮和指针。原始概念图由内置 imagegen 生成并保存在 [brand/logo-generated.png](../../brand/logo-generated.png)；生产 SVG 与 16/32/48/128px PNG 按生成稿描摹和渲染，横向字标与 GitHub 社交预览同源。此前“折角书签”和“引号望远镜”方案被用户否决，未进入发布资产。
- `node scripts/render_brand.mjs` → 退出 0；`sips -g pixelWidth -g pixelHeight public/icon-*.png brand/social-preview.png` → 图标 16/32/48/128px，预览 1280×640；人工查看 128px 图标和预览图无裁切。
- `pnpm typecheck`、`pnpm lint`、`python3 scripts/check_docs.py` → 通过；`pnpm test` → 4 文件 11 项通过，10,000 条纯领域规则筛选 p95 288.3ms；`pnpm test:e2e` → 真实构建扩展在 Chromium 的模拟素材流程 1 项通过，刷新三张运行截图；`pnpm zip` → 117.27 kB；`unzip -t` → 无压缩错误。
- `package.json` 与生成的 Manifest 版本同为 `0.1.0`，Manifest 指向四档图标。`.gitignore` 覆盖依赖、构建、测试报告、环境文件和常见密钥文件；对工作树与已有提交的常见凭据模式扫描无命中。此扫描不能代替完整安全审计。
- `gh auth status` 在本机确认活动账号 `WaterDJiang`，目标 `WaterDJiang/topic-hunter` 创建前不存在；`gh api repos/pnpm/action-setup/git/ref/tags/v6` 返回 `refs/tags/v6`。
- `gh repo create WaterDJiang/topic-hunter --public --source=. --remote=origin --push` → [公开仓库](https://github.com/WaterDJiang/topic-hunter)；`gh repo view` → `visibility: PUBLIC`、默认分支 `main`。首次推送本地与远端 SHA 同为 `fc4da712aab9f2e8239d9d4c1b77b678c7301280`。
- [首次 GitHub CI](https://github.com/WaterDJiang/topic-hunter/actions/runs/34740321620) → verify 全部通过，包括干净环境 `pnpm install --frozen-lockfile`、文档、类型、Lint、单测、构建、ZIP 和 Chromium e2e。真实 X 与真实 AI 仍未测试。
- 最终记录推送时执行 `git push origin main` 返回 `cannot lock ref 'refs/heads/main': is at a089bc3... but expected fc4da71...`；随即用 `git ls-remote` 确认远端实际已到本地 `a089bc3`，`git fetch origin main` 更新本地跟踪引用。推断为同次更新的远端引用竞争，未丢失提交。
- [后续 CI](https://github.com/WaterDJiang/topic-hunter/actions/runs/34740456707) 的 `pnpm test` 失败，原文 `expected 644.968246 to be less than or equal to 500`，10,000 条筛选 p95 645.0ms，GitHub Runner Node v22.23.2；本机同测试为 288.3ms。定位到每次规则判断新建 `Intl.Segmenter` 且同帖重复计数字符。改为模块级复用并在同帖筛选内传递字符数，本机 `pnpm test` 11 项通过，p95 91.7ms；`pnpm typecheck`、`pnpm lint`、`pnpm test:e2e`、`pnpm zip` 重新通过，新 ZIP 117.32 kB。[修复提交的 CI](https://github.com/WaterDJiang/topic-hunter/actions/runs/34740581825) 与 [v0.1.0 标签 CI](https://github.com/WaterDJiang/topic-hunter/actions/runs/34740648673) 均通过，远端 `main` 和标签都指向 `1a12899c380d63eff4312464ca53497a7fbdc99a`。

## M0 文档工程

- 目标：为新建 Topic Hunter 项目建立可执行的 Harness 与可追溯的产品依据。
- 已确认：公开开源工具；随浏览捕获；AI 选题卡；侧栏＋全页；短内容高互动兼顾小号；API 与复制提示词双模式；WXT＋React＋TypeScript；你主导、AI 开发。
- 现状证据：项目初始为空目录，且未初始化 Git；没有 `package.json`、插件源码或可执行 `pnpm` 脚本。
- 调研证据：六个开源仓库的 README、部分源码和 Chrome/WXT 官方文档；未安装运行候选项目，X 实时响应与兼容性待 M1 测试，详见 [调研记录](../ref/2026-09-13-open-source-landscape.md)。
- 产物：[产品规格](../../specs/2026-09-13-topic-hunter-v1.md)、[架构](../design/2026-09-13-extension-architecture.md)、[UI](../design/2026-09-13-ui-experience.md)、[计划](../plan/2026-09-13-m0-m4-roadmap.md)及索引。
- 变更理由：将长期 Agent 约束、有效产品行为、设计决策、分期状态分开存放，避免把计划中的能力当作已实现。
- 权限决策：按 Chrome Storage 官方文档，`storage.local` 默认对内容脚本可见；在架构与 Harness 写明 `TRUSTED_CONTEXTS` 初始化约束，让 AI 密钥只被扩展可信上下文访问。
- Harness 核验：AGENTS.md 18 项适用检查全部通过；CLAUDE.md 用导入桥接保持单一规则来源。两份均少于 200 行。核验发生在临时草案写入前，写入后重新运行文档检查。
- 文档验证：运行 `python3 scripts/check_docs.py`，输出 `PASS: 16 Markdown files, local links and Harness syntax`；18 个文件无尾随空白或 Tab。第一次临时 Spec 映射检查输出 `FAIL: CAP-03, CAP-04, FIL-02, FIL-03, TOP-03`，原因是计划采用缩写 ID；写全后重跑，输出 `PASS: all 13 Spec IDs mapped into plan`。
- 限制：未做插件功能测试、浏览器实测或候选项目运行验证；仓库没有 `package.json`，不把后续 `pnpm` 脚本记为通过。
- 下一步：M1-01 创建工程骨架、锁定依赖、建立真实脚本，再验证 X 的实时响应。

## UI 参考图补充

- 用户确认要看界面图，并询问采集范围；已明确第一版仅在用户打开的 X 页面已加载内容上被动捕获，不监控未打开的帖子。
- 使用内置 imagegen 生成并人工检查三张概念图：X 侧栏、全页素材库、选题卡详情；针对示例指标与入选理由不一致、文案暗示“全网采集”的问题作定向修正。
- 选定图片存入 `doc/assets/`，由 [UI 设计](../design/2026-09-13-ui-experience.md)引用。每张尺寸为 1672×941；内容为虚构示例，不是运行截图或产品功能验收。

## 快速改写路径补充

- 用户指出部分素材只需快速改写，不需要进入选题库；明确选择“只复制改写稿，自己去 X 粘贴发布”。
- [v1 规格](../../specs/2026-09-13-topic-hunter-v1.md)增加 QCK-01/02/03：单帖快速改写、可编辑短推、仅复制正文、草稿本地恢复；多帖深挖选题路径保留。快速稿与选题卡分离，不调用 X 发帖接口。
- [架构](../design/2026-09-13-extension-architecture.md)增加 `QuickDraft` 和独立存储；[UI](../design/2026-09-13-ui-experience.md)增加侧栏编辑视图和两条主操作；[计划](../plan/2026-09-13-m0-m4-roadmap.md)增加 M0-05 与 M3-04，并复核既有 M0-03/04 及下游任务。
- 原有三张概念图先于这次需求变更，故标明其单入口与现行交互不同；新生成并检查了[快速改写概念图](../assets/2026-09-13-topic-hunter-quick-draft-concept.png)，仅用于视觉参考，仍非最终运行 UI。
- 文档验证：`python3 scripts/check_docs.py` → `PASS: 16 Markdown files, local links and Harness syntax`；Spec 共 16 个 ID，全部映射到计划；插件功能仍未实现。`git status --short` → `fatal: not a git repository (or any of the parent directories): .git`；原因是 M0 尚未初始化 Git，故本次不能提供 Git diff 状态。

## 0.1.0 本地开发版

- 用户授权按现行双路径 Spec 开发，界面可以有个性化特色。已实现 WXT＋React＋TypeScript 的 MV3 扩展：页面响应观察与隔离校验、后台归一化/IndexedDB、原帖标记、筛选与收藏、侧栏与全页库、快速改写/复制、选题卡、设置与 JSON 备份。
- 设计使用蓝色信号线、青柠编号贴纸与编辑纸张质感；[侧栏发现](../assets/2026-09-13-runtime-sidepanel-discover.png)、[快速改写](../assets/2026-09-13-runtime-sidepanel-quick.png)、[全页素材库](../assets/2026-09-13-runtime-workspace-library.png)是实际扩展在 Chromium 中使用虚构素材生成的截图。
- `pnpm typecheck` → 退出 0；`pnpm lint` → `Checked 32 files ... No fixes applied.`；`pnpm test` → 4 个文件、11 个测试通过，含 AI HTTPS/权限拒绝/请求字段。10,000 条纯规则筛选 5 次，本机 Node v23.2.0 最近一次 p95 288.6ms；这是领域层过滤测量，不包含 IndexedDB 读取与 React 渲染。
- `pnpm test:e2e` → 构建通过，1 个真实 Chromium 扩展测试通过：360px 侧栏无横向溢出、单帖编辑复制并刷新恢复、快速稿不自动生成选题、选题保存、模拟 GraphQL 观察、暂停/恢复、两个 X 标签同帖去重与多次观测、不额外请求 X、1,000 条虚拟列表 DOM 卡片少于 30。
- `pnpm zip` → `.output/topic-hunter-0.1.0-chrome.zip`，126.91 kB；`unzip -t` 所有成员均为 OK。包内资源已构建，但未在真实 X 登录浏览流程做人工加载验收。
- 一次沙箱 `pnpm test` 启动后超过 90 秒没有测试输出，手动终止，退出 130；在允许本机进程运行的环境重跑同一命令，8 项通过。此前沙箱 Chromium 启动报 `MachPortRendezvousServer Permission denied`，在本机环境重跑浏览器测试通过。两者属于沙箱执行限制，仍保留失败记录。
- 新增双标签用例第一次执行 `pnpm test:e2e` 时，`toBeVisible()` 等待第二标签正文超时；第二标签在观察脚本就绪前立即发送模拟响应。改为等待原帖标记后再发送，重跑通过。
- 快速稿保存队列首次执行 `pnpm typecheck` 报 `Type 'Promise<unknown>' is not assignable to type 'Promise<void>'`；改为等待命令后返回 `void`，重跑通过。AI 权限请求已放在点击同步链中，避免先等待存储导致用户手势丢失。
- [CI](../../.github/workflows/ci.yml)、[隐私说明](../../PRIVACY.md)、[README](../../README.md)已写入；远程 CI 未运行。真实 X 五类页面、30 分钟浏览、与 x-viral-monitor 同启、真实 AI 供应商/拒权、20 组用户盲评仍待验收，不因模拟测试通过而标完成。
