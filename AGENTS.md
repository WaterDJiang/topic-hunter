# Topic Hunter Agent 约定

项目定位：面向内容创作者的开源 Chrome 插件，在正常浏览 X 时发现高表现短内容，单帖快速改写复制，多帖深挖成选题。

## 当前事实与入口

- 工程已有 WXT、React、TypeScript 源码与锁文件；实现进度、尚未完成的真实 X/AI/用户验收，以当前 Plan 和 Log 为准。不得把模拟页面验证写成真实 X 兼容性完成。
- 开始任务先读 specs/INDEX.md、doc/plan/INDEX.md、doc/log/INDEX.md；按索引只读相关分片。产品行为以有效 Spec 为准，细节设计看 doc/design/INDEX.md，外部方案看 doc/ref/INDEX.md。
- 检查命令：`pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm test:e2e`、`pnpm build`、`pnpm zip`、`python3 scripts/check_docs.py`。浏览器 e2e 使用本机 Chromium，运行前先构建。

## 项目特有规则

1. **先调研再自研**：新增采集、评分、导出或 AI 能力前查 doc/ref/；复用上游源码时记录固定提交、文件级授权与保留声明。业务差异化的选题闭环由本项目实现。
2. **指标有出处**：保存观测时间、来源和可空原始指标。缺失不能写成 0；平均热度不能称为近期增长；后者必须来自两次有效观测并展示区间。计数回落不截成增长 0。
3. **只观察正常浏览**：采集只用 X 已加载的帖子数据；不得自动滚动、刷新、追加 X 数据请求或传递 Cookie、Authorization、CSRF、请求头及完整响应体。引用帖与原帖正文分开。
4. **AI 用户触发**：只在用户点击后把所选素材发送到用户配置并授权的主机；API 密钥不进入 X 页面消息、日志、JSON 备份或版本库。Chrome storage.local 初始化时限制为 TRUSTED_CONTEXTS，不让内容脚本直接读取密钥。拒权或无配置时保留复制提示词路径；关闭页面不自动重试收费请求。
5. **证据与推测分开**：快速稿和选题卡保留原帖链接与指标；传播原因标推测，不编造个人经历、评论内容或外部事实。快速稿只复制编辑正文，用户自行到 X 粘贴发布，不自动进入选题库。没有完成用户盲评时不宣称选题效果达标。

## 架构与 UI 纪律

- 目标栈为 Chrome MV3、WXT、React、TypeScript、pnpm；依赖版本和构建命令须在 M1 锁文件及 package.json 出现后核实。采集、校验、归一化、持久化、规则、提示词、UI 分职责；纯领域逻辑不依赖 React。
- React 组件按侧栏、素材库、快速稿、选题库功能域划分。新增共享组件前先查现有语义；改共享组件时回归所有调用处。超过 300 行、条件嵌套 3 层、同语义重复 2 次只触发职责评审，不机械拆分。
- 详细颜色、字型、间距只维护在 doc/design/2026-09-13-ui-experience.md。UI Spec 写明加载、空、错误、成功、禁用状态及键盘、焦点、窄屏验收。侧栏主要流程保持三次操作内；不能用颜色单独表示命中。
- D-S-T-E：盲测辨认候选/收藏/采用；预设让新用户直接开始；指标与错误文案写出数值和下一动作；异步生成可取消，新候选不打断阅读。

## 文档与持续开发

- specs/ 保存唯一有效产品行为与验收；doc/design/ 保存架构和 Token；doc/ref/ 保存来源与复用依据；doc/plan/ 保存任务依赖和状态；doc/log/ 保存决策、命令、结果与缺口。进度不得写入本文件。每个增长目录先读 INDEX，新增分片后同步 INDEX。
- 先定义 Spec 与验收，再实施 feature、fix 或 refactor。仅改变已授权范围内的实现顺序时更新 Plan 后继续；用户明确改变行为时先同步 Spec 和受影响设计。未决产品取舍只阻塞相关任务，独立任务继续。
- 任务使用稳定 ID；局部变化只替代相关任务并指向新 ID，下游和已完成但受影响任务标待复核；明确已有实现保留、修改、迁移或移除。旧测试结果不能证明新规格。
- 中断时在当前 Plan 留有效依据、完成/待复核、阻塞、验证证据与下一条可执行任务；恢复时核对实际文件与依赖，不执行已替代、已废弃或依赖未满足的任务。
- 记录关键判断或修正的截图到 doc/assets/，由当日日志引用；没有截图不补造。报错写触发命令、原文、推断原因和未完成范围，不静默重试。
- 只改当前任务必需的代码与文档；若更简单的实现满足同一验收，直接说明并采用。提交前跑存在的检查，未跑的检查明确标记。公开仓库或商店发布属于 M4 的单独动作。
